import Foundation

class addOperation { // class를 사용해서 서로 코드를 분리함
    func oparate(firstNumber: Double, secondNumber: Double) -> Double {
        return firstNumber + secondNumber
    } // 덧셈 계산 정의
}
